export class ProgrammingLanguage {
  constructor(
    public id: string,
    public name: string,
    public paradigm: string
  ) {}

  //   hte chin yin hte lh ya
  //   getDescription(): string{
  //     return `${this.name} is a ${this.paradigm} programming language.`;
  //   }
}
